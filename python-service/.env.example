# ── LLM Provider ─────────────────────────────────────────────────────────────
# Must match AI_PROVIDER in the Next.js .env.local
# Options: anthropic | openai | grok | openrouter
AI_PROVIDER=grok

# ── Direct provider keys (pick the one matching AI_PROVIDER) ─────────────────
XAI_API_KEY=xai-...                  # AI_PROVIDER=grok
# ANTHROPIC_API_KEY=sk-ant-...       # AI_PROVIDER=anthropic
# OPENAI_API_KEY=sk-...              # AI_PROVIDER=openai

# ── OpenRouter — route to any model via one key ───────────────────────────────
# AI_PROVIDER=openrouter
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=x-ai/grok-3       # smart tier model (default: anthropic/claude-sonnet-4-6)
