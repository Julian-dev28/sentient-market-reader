# ── LLM Provider ────────────────────────────────────────────────────────────
# Must match AI_PROVIDER in the Next.js .env.local
AI_PROVIDER=grok    # openrouter | anthropic | openai | grok

# OpenRouter (recommended — routes to any model via one key)
# Get a key at https://openrouter.ai
OPENROUTER_API_KEY=sk-or-v1-...

# Direct providers (optional — use if not using OpenRouter)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
XAI_API_KEY=xai-...

